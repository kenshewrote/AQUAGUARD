# Runtime dependencies for deployment (Render/Railway/etc).
# requirements-lstm.txt is the local-dev version (also includes pytest for tests);
# this file is the one hosting platforms auto-detect, trimmed to what app.py needs
# to actually serve requests, plus gunicorn as the production WSGI server.
torch>=2.0
pandas>=2.0
numpy>=1.24
flask>=3.0
flask-cors>=4.0
openpyxl>=3.1
gunicorn>=21.0
